<div class="container col-lg-4">
	<div class="row justify-content-center">
		<img src="https://st3.depositphotos.com/1007168/14192/v/950/depositphotos_141921256-stock-illustration-burger-cartoon-character.jpg" alt="Girl in a jacket" width="300" height="300">
	</div>
	<h1 class="row justify-content-center">Burgers to the Moon!</h1>

<div class="row">
  <div class="col-lg-6">
    <h2>Burgers I hate</h2>
    <ul>
      {{#each burgers}}
      {{#unless good}}
      {{> burgers/burger-block good=true}}
      {{/unless}}
      {{/each}}
    </ul>
  </div>
  
  <div class="col-lg-6">
    <h2>Yummy Burgers</h2>
    <ul>
      {{#each burgers}}
      {{#if good}}
      {{> burgers/burger-block good=false}}
      {{/if}}
      {{/each}}
    </ul>
  </div>
</div>


<form id="create-form" class="justify-content-center">
    <div class="row col lg-2 form-group">
        <label for="ca"></label>
        <textarea type="text" id="bu" name="name" rows="4" cols="40">
        
        </textarea>
    </div>
    <div class="btn d-flex justify-content-center">
        <button type="submit">Submit</button>
    </div>
    <div class="d-flex justify-content-center">
        <p>Enter a burger that makes you all happy on the inside.  Then hit the button</p>
    </div>
</form>

</div>